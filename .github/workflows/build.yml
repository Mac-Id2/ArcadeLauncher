name: Build Arcade Launcher

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build auf ${{ matrix.artifact_name }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: windows-x64
          - os: ubuntu-22.04
            artifact_name: linux-x64
          - os: macos-latest
            artifact_name: macos-arm64
          - os: macos-latest
            artifact_name: macos-x64

    steps:
      - name: Code auschecken
        uses: actions/checkout@v4

      - name: Python installieren
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Abhängigkeiten installieren
        shell: bash
        run: |
          if [ "${{ matrix.artifact_name }}" = "macos-x64" ]; then
            arch -x86_64 python -m pip install --upgrade pip
            arch -x86_64 pip install "setuptools==68.2.2" pygame pyinstaller pynput
          else
            python -m pip install --upgrade pip
            pip install "setuptools==68.2.2" pygame pyinstaller pynput
          fi

      - name: PyInstaller Build
        shell: bash
        run: |
          mkdir -p dist/final_delivery
          
          # Build-Befehl (Name wird hier groß geschrieben: Launcher)
          if [ "${{ matrix.artifact_name }}" = "macos-x64" ]; then
            arch -x86_64 pyinstaller --noconsole --onefile --collect-all pynput --name Launcher launcher.py
          else
            pyinstaller --noconsole --onefile --collect-all pynput --name Launcher launcher.py
          fi
          
          # --- KORREKTUR: Pfade und Endungen ---
          if [ "${{ runner.os }}" = "Windows" ]; then
            # Windows behält Launcher.exe
            mv dist/Launcher.exe dist/final_delivery/
          else
            # Linux und Mac behalten Launcher (ohne Endung)
            mv dist/Launcher dist/final_delivery/
            chmod +x dist/final_delivery/Launcher
          fi
          
          # Assets und JSON kopieren
          [ -d assets ] && cp -r assets dist/final_delivery/
          [ -f games.json ] && cp games.json dist/final_delivery/

      - name: Archivieren
        shell: bash
        run: |
          cd dist/final_delivery
          if [ "${{ runner.os }}" = "Linux" ]; then
            # Speziell für Linux nutzen wir den nativen Zip-Befehl um Rechte zu wahren
            zip -r "../../Launcher-${{ matrix.artifact_name }}.zip" .
          else
            # Windows/Mac nutzen das Python-Script zum Archivieren
            python -c "import shutil; shutil.make_archive('../../Launcher-${{ matrix.artifact_name }}', 'zip', '.')"
          fi

      - name: Artifact hochladen
        uses: actions/upload-artifact@v4
        with:
          name: Launcher-${{ matrix.artifact_name }}
          path: Launcher-${{ matrix.artifact_name }}.zip
          retention-days: 7

  release:
    name: GitHub Release erstellen
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Alle Artifacts herunterladen
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Release erstellen und Assets hochladen
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
