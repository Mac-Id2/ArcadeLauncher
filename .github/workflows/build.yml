name: Build Arcade Launcher

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build auf ${{ matrix.artifact_name }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: windows-x64
          - os: ubuntu-22.04
            artifact_name: linux-x64
          # macOS 14+ (macos-latest) nutzt Apple Silicon (ARM64 / M1, M2...)
          - os: macos-latest
            artifact_name: macos-arm64
          # macOS 13 läuft nativ auf Intel-Servern. 
          # Viel sicherer als der Rosetta-Trick auf einem ARM-Server!
          - os: macos-13
            artifact_name: macos-x64

    steps:
      - name: Code auschecken
        uses: actions/checkout@v4

      - name: Python installieren
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Abhängigkeiten installieren
        shell: bash
        run: |
          python -m pip install --upgrade pip
          # WICHTIG: pynput hinzugefügt, damit der AFK-Watchdog mit einkompiliert wird!
          pip install "setuptools==68.2.2" pygame pynput pyinstaller

      - name: PyInstaller Build
        shell: bash
        run: |
          # Ordnerstruktur für Artifact vorbereiten
          mkdir -p dist/final_delivery
          
          # Da wir jetzt saubere OS-Images nutzen, brauchen wir keine if/else Logik mehr für den Build
          pyinstaller --noconsole --onefile launcher.py
          
          # Binärdatei verschieben (Name unterscheidet sich je nach OS)
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv dist/launcher.exe dist/final_delivery/
          else
            mv dist/launcher dist/final_delivery/
            # WICHTIG: Ausführungsrechte explizit setzen
            chmod +x dist/final_delivery/launcher
          fi
          
          # Assets kopieren
          [ -d assets ] && cp -r assets dist/final_delivery/
          [ -f games.json ] && cp games.json dist/final_delivery/

      - name: Archivieren
        shell: bash
        run: |
          cd dist/final_delivery
          if [ "${{ runner.os }}" = "Linux" ]; then
            zip -r "../../Launcher-${{ matrix.artifact_name }}.zip" .
          else
            python -c "import shutil; shutil.make_archive('../../Launcher-${{ matrix.artifact_name }}', 'zip', '.')"
          fi

      - name: Artifact hochladen
        uses: actions/upload-artifact@v4
        with:
          name: Launcher-${{ matrix.artifact_name }}
          path: Launcher-${{ matrix.artifact_name }}.zip
          retention-days: 7

  release:
    name: GitHub Release erstellen
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Alle Artifacts herunterladen
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Release erstellen und Assets hochladen
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}